# M3 评审记录（raw 层，非门里程碑）

- 日期：2026-09-04
- 里程碑类型：非评审门（findings 清零即继续，无需用户确认）
- 评审对象：commits 670f2aa..81db959（main @ 81db959）
- 验证证据：CI 全绿链 33910814615（冷编译失败——dist 目录缺失 ENOENT，be1ea1f 修复）→ 33911096542（三 job 绿，outDir 修复验证）→ 33913374420（三 job 绿，Test 守卫 + pixDestroy 修复后 raw 测试真实执行）→ 33914534570（三 job 绿，chatroom 修复轮）；本地 npm run typecheck 双域 + npm test 5/5 + node --check 双脚本全绿
- 人格选定：主持方直接选定（用户 2026-09-03 授权免逐次确认）
- 执行通道：multi-agent 直派（trellis-check 第一层 + 三视角第二层），派即监、监即读

## 第一层：trellis-check 硬评审

**结论：三条清单项达标，但发现 2 blocker（均已就地修复）+ 1 warning（已修复）。**

- 清单 1（gen-exports.mjs 完整化）：renderLooseDts 松 d.ts 生成 + --dts-out 落地；consumer fixture 编译验证（tsc --noEmit --strict 通过）
- 清单 2（src/raw/ 危险区）：danger zone 文档完备；ambient 声明荷载测试（移除 → typecheck 红，证明承重）
- 清单 3（tsconfig 三域）：web 域 DOM lib 含 55 处 WebAssembly 声明确认；exports/files/sideEffects 骨架与首个 src 文件原子落地（670f2aa 单提交）

**第一层 findings（fixed）**：

- B1 CI Test 步骤位于 Build (full ABI) 之前——dist/ gitignored、deps 缓存只恢复 tmp/，describe.skipIf 静默跳过，CI 全绿对 raw 测试是空洞证据。修复：Test 挪至 full-abi 构建 + 冒烟后，前置 test -f 硬守卫（0d55921）
- B2 _pixDestroy(pix) 传 PIX*，但 pixDestroy(PIX **ppix) 收 PIX**——静默 UB（实测 bogus free + 宽度字段覆写）。修复：malloc 槽位间接形态 + 注释说明（0d55921）
- W1 Promise.race 失败侧信道零测试覆盖。修复：新增垃圾 bytes rejects 测试（0d55921）
- .git/info/exclude 补 mytokens.txt（本地排除，未 track；按凭据纪律未读内容）

**第一层 findings（not fixed，转移或豁免）**：

- W2 松 d.ts 无 typecheck 守卫 → 豁免：M4 清单已有 consumer fixture + arethetypeswrong 条目（M0 F16），天然覆盖，现在加是重复守卫
- N1 本地 dist 产物过期（15:19 副本缺 leptonica-raw.d.ts）→ 已解决：33913374420 artifact 重新下载至本地 dist，含全部六文件
- N2 run 33911096542 的 raw 测试证据无效 → 已闭环：33913374420 raw 测试真实执行（硬守卫在场）
- N3 EmscriptenModuleArg 类型双写 → 与第二层 N3 合并，记 backlog

## 第二层：三视角并行独立评审

人格：Sebastian McKenzie（API 设计 / DX）、Ted Kremenek（危险边界文档）、Anders Hejlsberg（类型松紧权衡）。独立评审、互不通信；主持方代码级核实（误报预算规则）。三个视角独立收敛到同三个包边界问题。

### API 设计 / DX（Sebastian McKenzie 框架）——有 blocker

- B1 exports 缺 ./full-abi/leptonica.wasm 子路径：loadRaw 必填 wasmBinary，docstring 指路 dist/full-abi/leptonica.wasm，但该子路径未列入 exports（ERR_PACKAGE_PATH_NOT_EXPORTED）。用户只能对 .mjs resolve 后做路径手术。修复：一行 exports 条目
- W1 wasmBinary 缺失时报错不提 wasmBinary 不提本包（裸 WebAssembly.instantiate TypeError）。修复：前置校验 + 报错给出获取路径
- W2 整包无 README。记 M6（README 三段式是 M6 既定条目）
- N1 EmscriptenModuleArg 双写漂移（与 Ted N3 / Anders N3 合并）
- N2 index.ts 注释指错子路径（说 see ./raw，实际 import ./full-abi/leptonica.mjs）
- N3 as BufferSource 多余
- 正面确认：双 loadRaw 独立实例（无隐藏单例）；垃圾 bytes reject 干净；rawMemory getter 增长安全

### 危险边界文档（Ted Kremenek 框架）——0 blocker

- W1 跨实例指针串用未文档化：每次 loadRaw 独立实例化独立堆，A 实例 PIX* 传 B 的 _pixGetWidth 返回 0 静默无异常——C 里不存在的坑。修复：DANGER ZONE 补条目
- W3 C 字符串封送零文档：const char* 形参（_getImpliedFileFormat 等）需要手写 malloc+UTF-8+NUL，漏 NUL 即堆越读。修复：DANGER ZONE 补条目
- N1 PIX** 出参 staging 约定只在测试注释里。修复：DANGER ZONE 补条目
- N2 types.ts 堆增长描述失真（实测 resizable ArrayBuffer，旧视图仍有效——保守措辞对但机制错）。修复：措辞更正 + getter 热循环成本警告
- N3 评审包称 5 个 raw 测试实为 4 个 it + 1 M0 smoke——记录澄清
- 正面确认：失败侧信道干净（无 pending 泄漏、无 unhandled rejection、事件循环正常）；2745 符号零缺失；typo 符号运行时 undefined 是文档明示的松类型代价

### 类型松紧权衡（Anders Hejlsberg 框架）——有 blocker

- B1 exports types 指向 .ts 源，外部消费者必现 TS5097（实测三配置：NodeNext 裸配 / +skipLibCheck / bundler+skipLibCheck 全红；skipLibCheck 救不了——它只跳 .d.ts，.ts 被拉进程序）。→ **主持方裁决：warning 豁免 + 转移 M4**。理由：(a) source-direct 发布模型是 design 既定决策，不是 M3 引入的缺陷；(b) 消费者视角验证是 M4 F16 既定职责（consumer fixture + arethetypeswrong），现在修需要引入构建发射（.d.ts 生成管线），与零构建纪律和 M4 边界冲突；(c) 三个评审者都指出 M4 F16 应吸收此问题。豁免条件：M4 落地 consumer fixture 时 types 入口问题一并解决，不得带入 M6 发布
- W1 两套互不相容类型无编译时约束（RawModule 索引签名 vs 生成 RawSymbols 2745 具名属性；as unknown as 双 cast 绕过矛盾）→ 同上转移 M4（与 B1 同根：需要类型发射基础设施）
- W2 松 d.ts 无 CI 漂移守卫 → 与第一层 W2 合并，M4 consumer fixture 吸收
- W3 instantiateWasm 失败后 factory promise 永久悬垂（重复失败积累闭包，低危）→ 记 backlog，M5 Worker 生命周期评审时复核
- N1 测试 as RawFunction cast 多余（union 可直接调用）→ 记 backlog（机械清理，下次碰该文件时顺手）
- N2 _${string} 索引签名承诺一切下划线键都是函数（比已知 2745 名宽）→ 与 W1 同根
- N3 EmscriptenModuleArg 双写（三视角合并）
- 正面确认：wasmBinary 必填让 memory 捕获在类型上成立；race 回归测试锁住设计；松紧判断正确

## 主持方裁决与修复轮

**修复（81db959，CI 33914534570 三 job 绿）**：

1. exports 补 ./full-abi/leptonica.wasm 子路径（三视角共同 blocker——loadRaw 是 M3 交付物，其必填输入无法通过包获取 = 交付不完整，属 M3 范围内必修）。实测：import.meta.resolve 解析成功、bytes 可读、未知子路径仍正确 ERR_PACKAGE_PATH_NOT_EXPORTED
2. loadRaw 前置校验 wasmBinary（缺失/空 → 带获取路径的可诊断报错）
3. DANGER ZONE 补四条：跨实例指针隔离、C 字符串封送（malloc+UTF-8+NUL）、T** 出参 staging、堆增长措辞更正 + 热循环成本警告
4. index.ts 注释补 wasm 子路径指引（顺带修 N2 指错问题）

**豁免（warning，书面理由）**：

- types 指向 .ts 源（TS5097）+ 两套类型无编译时约束 + 松 d.ts 无 typecheck 守卫：三问题同根（需要类型发射基础设施），M4 F16 consumer fixture 是既定解决场所。豁免条件：M4 解决，不带入 M6。若 M4 未解决则升级 blocker

**M2-F2 再裁决（本次评审正式落地）**：

设计 §4.3 明确 raw 层全量 C ABI 可用，PRD 决策 ②/⑦ 确立 full-abi 产物随包发布（逃生舱实体）。M2 F2「npm pack 排除 full-abi」的原始关切是**默认产物不含解码面**（default 产物承诺），不是包范围承诺。精确语义：dist/（default）无解码入口面向普通用户；dist/full-abi/ 完整 C ABI 随包发布服务逃生舱。M6 措辞回填：「npm pack 审查确认 default 产物不含 pixRead*/png_create_read_struct 等解码符号」而非「包不含 full-abi 产物」。

**记 backlog（nit，不阻塞）**：

- EmscriptenModuleArg 类型双写（三视角共同 nit；修复需生成器 import src 类型，模块边界判断题，M4 API 设计评审定夺）
- 测试 as RawFunction cast 多余（机械清理）
- factory promise 失败悬垂（低危闭包积累，M5 Worker 生命周期复核）
- 垃圾 bytes 测试不锁错误文案（实测错误信息好，值得断言住防退化）
- heap 视图缺 F32/F64（leptonica float API 常见）

## 过门判定

非门里程碑，findings 清零（2 blocker 修复 + 1 blocker 豁免转移 M4 带条件 + warnings 修复/豁免/转移 + nits 记 backlog）。M3 通过，进 M4。
